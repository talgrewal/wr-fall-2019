import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  xxxIcon: {
    resizeMode: 'contain',
    // width: 40,
    height: 35,
  },
  categoryImage: {
    // width: 40,
    height: 50,
    resizeMode: 'contain',
  },
});
export default styles;
